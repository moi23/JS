/**
 *  Variaveis em JavaScript
 *
 *  É uma forma de armazenar dados na memória do computador
 */

// Exemplo:

var nome = "João"; // String
var idade = 20; // Number
var peso = 80.5; // Number
var humano = true; // Boolean
var alunos = ["João", "Pedro", "Maria"]; // Array
var aluno = { nome: "João", idade: 20, peso: 80.5, humano: true }; // Object
var alunos = null; // Null
var alunos = undefined; // Undefined
var alunos = function soma(a, b) {
  return a + b;
}; // Function
var alunos = 2 + 2; // Expressão
var alunos = 2 == 2; // Expressão
var alunos = 2 > 2; // Expressão
var alunos = 2 < 2; // Expressão
var alunos = 2 >= 2; // Expressão
var alunos = 2 <= 2; // Expressão
var alunos = 2 != 2; // Expressão
var alunos = 2 === 2; // Expressão
var alunos = 2 !== 2; // Expressão
var alunos = 2 && 2; // Expressão
var alunos = 2 || 2; // Expressão
var alunos = 2 + 2 == 4; // Expressão
